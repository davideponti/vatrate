{
  "name": "vatrate",
  "version": "1.0.0",
  "private": true,
  "description": "Open EU VAT rates API — Know your VAT, ship globally.",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "validate": "node apps/data/scripts/validate.mjs",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "workspaces": ["apps/*", "packages/*"],

  "devDependencies": {
    "prettier": "^3.2.0",
    "turbo": "^1.13.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
