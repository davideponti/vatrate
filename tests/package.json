{
  "name": "@vatrate/tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@vatrate/api": "*",
    "@vatrate/shared": "*"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "typescript": "^5.4.0"
  }
}